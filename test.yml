---
- name: Configure the login banner
  hosts: Routers
  tasks:

  -name: Add Banner
  cisco.ios.ios_banner:
    banner: login
    text: |
      this is my login banner
      that contains a multiline
      string
    state: present
